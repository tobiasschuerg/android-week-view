<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".SampleActivity"
            android:label="WeekView Demo - Choose Implementation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ClassicWeekViewActivity"
            android:label="Legacy View-based WeekView (Deprecated)"
            android:exported="true" />
        <activity
            android:name=".ComposeWeekViewActivity"
            android:label="Modern Compose WeekView (Recommended)"
            android:exported="true"
            android:theme="@style/Theme.App.NoActionBar" />
    </application>

</manifest>
